# Subdomain Finder

A fast CLI tool that finds subdomains using passive sources (cert transparency, search engines) and active techniques (bruteforce, permutations). Works great on Ubuntu.

## Install

1. Clone the repo:
```bash
git clone https://github.com/21CasterisK/subdomain-finder.git

cd subdomain-finder
